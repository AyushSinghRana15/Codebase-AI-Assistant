# Embeddings — Vector Store

Handles embedding generation and FAISS vector retrieval.

| File | Description |
|------|-------------|
| `embedder.py` | Sentence-transformer model wrapper, batch encoding, FAISS index builder |
| `retriever.py` | FAISS index loader, similarity search, chunk metadata access |
